# 并行执行系统用户指南

## 概述

并行执行系统是 Claude Code 工作流的核心增强功能，通过同时调用多个外部模型（Codex 和 Gemini）完成不同维度的分析、生成和审查任务，显著提升工作流执行效率。

**核心优势**:

- ⚡ **性能提升**: 平均节省 50% 执行时间
- 🎯 **多维视角**: Codex 和 Gemini 各司其职，交叉验证
- 🔄 **断点恢复**: 支持中断后继续执行
- 📊 **实时反馈**: 统一进度显示，随时掌握任务状态

---

## 快速开始

### 什么是并行执行？

传统串行模式下，工作流需要依次调用外部模型：

```
Phase 1: Codex 分析 (2分钟)
  ↓
Phase 2: Gemini 分析 (2分钟)
  ↓
Phase 3: 合并结果
---
总耗时: ~4分钟
```

并行模式下，两个模型同时执行：

```
Phase 1: Codex 分析 (2分钟) ║
                            ║ 并行
Phase 2: Gemini 分析 (2分钟)║
  ↓
Phase 3: 合并结果
---
总耗时: ~2分钟 (节省 50%)
```

### 哪些 Orchestrator 支持并行？

| Orchestrator | 并行阶段   | Codex 职责                   | Gemini 职责                    |
| ------------ | ---------- | ---------------------------- | ------------------------------ |
| dev          | Phase 2, 5 | 后端/逻辑分析、后端代码生成  | 前端/UX分析、前端代码生成      |
| debug        | Phase 2, 4 | 后端/逻辑假设、安全/性能修复 | 前端/UX假设、可读性/维护性修复 |
| test         | Phase 2    | 后端/单元测试                | 前端/集成测试                  |
| plan         | Phase 4    | 技术准确性审查               | 可读性审查                     |
| review       | Phase 3    | 安全/性能审查                | UX/可访问性审查                |
| social-post  | Phase 3    | 技术深度草稿                 | 通俗易懂草稿                   |
| ui-ux-design | Phase 3    | 极简风格设计                 | 现代/创意风格设计              |

**顺序执行** (无并行):

- commit-orchestrator
- image-orchestrator

### 第一个并行工作流

让我们通过一个简单的例子体验并行执行：

```bash
# 1. 启动开发工作流
/developing:dev-orchestrator

# 2. 输入任务描述
"添加用户登录功能"

# 3. 观察并行执行
# Phase 2 会同时启动两个后台任务：
# - codex-feature-analysis (后端/逻辑分析)
# - gemini-feature-analysis (前端/UX分析)

# 4. 查看实时进度
🔄 并行执行中 (Phase: feature_analysis)

┌────────────────────────────────────────────────────────────┐
│ codex-feature-analy  [████████░░] 🔄 运行中 (1m 23s) [codex] │
│ gemini-feature-anal  [██████████] ✅ 完成   (1m 05s) [gemini] │
└────────────────────────────────────────────────────────────┘

📊 统计: 活跃 1/2 | 完成 1 | 失败 0 | 总计 2

# 5. 等待完成后，Claude 会合并两个模型的分析结果
```

---

## Orchestrator 详细指南

### dev-orchestrator (开发编排器)

**用途**: 实现新功能、重构代码、架构设计

**并行阶段**:

- **Phase 2 - 功能分析**: Codex 分析后端需求，Gemini 分析前端需求
- **Phase 5 - 代码生成**: Codex 生成后端代码，Gemini 生成前端代码

**使用示例**:

```bash
/developing:dev-orchestrator

# 用户输入:
"实现购物车功能，支持添加/删除商品，计算总价"

# 执行流程:
Phase 1: 需求分析 (Claude)
Phase 2: 功能分析 (Codex ║ Gemini) ← 并行
Phase 3: 设计方案 (Claude)
Phase 4: 实施规划 (Claude)
Phase 5: 代码生成 (Codex ║ Gemini) ← 并行
Phase 6: 质量检查 (Claude)
Phase 7: 最终交付
```

**输出**:

- `.claude/developing/analysis-codex.md` - 后端分析
- `.claude/developing/analysis-gemini.md` - 前端分析
- `.claude/developing/implementation.md` - 最终实现代码

---

### debug-orchestrator (调试编排器)

**用途**: 定位和修复 Bug

**并行阶段**:

- **Phase 2 - 假设生成**: Codex 生成后端/逻辑假设，Gemini 生成前端/UX假设
- **Phase 4 - 修复方案**: Codex 提供安全/性能修复，Gemini 提供可读性/维护性修复

**使用示例**:

```bash
/debugging:debug-orchestrator

# 用户输入:
"用户点击提交按钮后，表单没有提交"

# 执行流程:
Phase 1: 症状收集 (Claude)
Phase 2: 假设生成 (Codex ║ Gemini) ← 并行
Phase 3: 假设验证 (Claude)
Phase 4: 修复方案 (Codex ║ Gemini) ← 并行
Phase 5: 修复实施 (Claude)
Phase 6: 验证测试
```

---

### review-orchestrator (审查编排器)

**用途**: 代码质量审查、PR 审查、安全审计

**并行阶段**:

- **Phase 3 - 代码审查**: Codex 审查安全/性能，Gemini 审查 UX/可访问性

**使用示例**:

```bash
# 审查当前 git 变更
/reviewing:review-orchestrator

# 或审查指定目录
/reviewing:review-orchestrator path=src/components/

# 执行流程:
Phase 0: 初始化
Phase 1: 安全扫描 (Claude)
Phase 2: 质量分析 (Claude)
Phase 3: 多模型审查 (Codex ║ Gemini) ← 并行
  - Codex: 安全漏洞、性能瓶颈、边界条件
  - Gemini: 用户交互、可访问性、响应式设计
Phase 4: 生成报告
```

**输出**:

- `.claude/reviewing/review-codex.md` - 安全/性能审查
- `.claude/reviewing/review-gemini.md` - UX/可访问性审查
- `.claude/reviewing/report.md` - 综合审查报告

---

### ui-ux-design-orchestrator (UI/UX 设计编排器)

**用途**: 界面设计、用户体验优化

**并行阶段**:

- **Phase 3 - 设计变体生成**: 3 个并发任务
  - Codex: 极简风格（Minimal）
  - Gemini: 现代风格（Modern）
  - Gemini: 创意风格（Creative）

**使用示例**:

```bash
/ui-ux-design:ui-ux-design-orchestrator

# 用户输入:
"设计一个 SaaS 产品的 Dashboard"

# 执行流程:
Phase 1: 需求分析
Phase 2: 样式推荐
Phase 3: 设计生成 (3变体并行) ← 并行
  - design-A.md: 极简风格
  - design-B.md: 现代风格
  - design-C.md: 创意风格
Phase 4: 用户选择变体
Phase 5: UX 检查
Phase 6: 代码生成
Phase 7: 质量验证
```

**特点**: 生成 3 个不同风格的设计方案供用户选择

---

## 状态管理

### V2 状态文件格式

所有 orchestrators 使用统一的 V2 状态文件格式，保存在 `.claude/<domain>.local.md`：

```yaml
---
workflow_version: "2.0"
domain: "developing"
workflow_id: "dev-20260113-143000"
goal: "实现购物车功能"
created_at: "2026-01-13T14:30:00Z"
updated_at: "2026-01-13T14:35:00Z"
current_phase: "feature_analysis"

# 并行执行控制
parallel_execution:
  max_concurrency: 8 # 全局最大并发数
  active_tasks: 2 # 当前运行任务数
  completed_tasks: 0 # 已完成任务数
  failed_tasks: 0 # 失败任务数

# 会话管理
sessions:
  codex:
    current: "session_abc123" # 当前 SESSION_ID
    history: ["session_xyz"] # 历史 SESSION_ID
  gemini:
    current: "session_def456"
    history: []

# 子任务列表
subtasks:
  - id: "codex-feature-analysis"
    backend: "codex"
    status: "running" # pending | running | completed | failed
    task_id: "task_789" # Claude Code 后台任务 ID
    started_at: "2026-01-13T14:30:00Z"
    completed_at: null
    output: null
    error: null

  - id: "gemini-feature-analysis"
    backend: "gemini"
    status: "completed"
    task_id: "task_790"
    started_at: "2026-01-13T14:30:00Z"
    completed_at: "2026-01-13T14:31:05Z"
    output: ".claude/developing/analysis-gemini.md"
    error: null


# ... 其他字段 ...
---
```

### 断点恢复机制

当工作流中断时（网络问题、系统重启等），系统会自动检测并提示恢复：

```
⚠️  发现未完成的工作流

Domain: developing
Phase: feature_analysis
进度: 1/2 任务完成

是否继续？
[Y] 继续 (推荐)
[N] 重新开始
```

**恢复原理**:

1. 读取状态文件中的 `task_id`
2. 使用 Claude Code 的 `TaskOutput` 工具获取后台任务结果
3. 更新状态并继续下一阶段

### SESSION_ID 管理

SESSION_ID 用于在多个阶段间保持外部模型的会话上下文。

**自动管理**:

- 每次调用外部模型时，系统自动提取并保存 SESSION_ID
- 下次调用同一模型时，自动传入 SESSION_ID 继续会话

**手动查看**:

```bash
# 查看当前 Codex 会话
yq eval '.sessions.codex.current' .claude/developing.local.md

# 查看会话历史
yq eval '.sessions.codex.history' .claude/developing.local.md
```

---

## 进度监控

### 统一进度显示接口

系统提供 5 个核心函数用于进度监控（基于 `progress.sh`）：

#### 1. print_parallel_progress - 详细进度

显示多任务进度，包含每个任务的状态、耗时、后端类型：

```bash
source "${CLAUDE_PLUGIN_ROOT}/skills/_shared/ui/progress.sh"

print_parallel_progress ".claude/developing.local.md"
```

输出:

```
🔄 并行执行中 (Phase: feature_analysis)

┌────────────────────────────────────────────────────────────┐
│ codex-feature-analy  [████████░░] 🔄 运行中 (1m 23s) [codex] │
│ gemini-feature-anal  [██████████] ✅ 完成   (1m 05s) [gemini] │
└────────────────────────────────────────────────────────────┘

📊 统计: 活跃 1/2 | 完成 1 | 失败 0 | 总计 2
```

#### 2. print_simple_progress - 简洁进度条

单行进度条，适合嵌入到其他输出：

```bash
print_simple_progress ".claude/developing.local.md"
```

输出:

```
🔄 进度: [████████████░░░░░░░░] 60% | 活跃: 1 | 完成: 1 | 失败: 0
```

#### 3. monitor_progress - 持续监控（阻塞）

持续显示进度，直到所有任务完成：

```bash
# 每2秒刷新一次
monitor_progress ".claude/developing.local.md" 2
```

**适用场景**: 等待并行任务完成，无需执行其他操作

#### 4. start_progress_monitor - 后台监控（非阻塞）

在后台启动进度监控，不阻塞主流程：

```bash
# 启动后台监控
MONITOR_PID=$(start_progress_monitor ".claude/developing.local.md" 2)

# 执行其他任务
# ...

# 停止监控
stop_progress_monitor "$MONITOR_PID"
```

**适用场景**: 需要同时执行其他任务

#### 5. stop_progress_monitor - 停止后台监控

停止后台监控进程：

```bash
stop_progress_monitor "$MONITOR_PID"
```

### 自动进度显示

**默认行为**: orchestrators 会在并行阶段自动显示进度，无需手动调用。

**查看进度**: 进度实时更新在终端，无需额外操作。

---

## 错误处理

### 错误处理原则

1. **不重试**: 后台任务失败后，不自动重试
2. **不降级**: 失败不会回退到串行模式
3. **记录失败**: 所有失败记录在状态文件和日志中
4. **用户决策**: 失败后由用户决定下一步操作

### 常见错误和解决方案

#### 错误 1: 后台任务启动失败

**症状**:

```
❌ 无法启动后台任务: codex-feature-analysis
```

**原因**:

- 达到并发槽位上限（8个）
- codeagent-wrapper 不可用
- 网络连接问题

**解决方案**:

```bash
# 1. 检查活跃任务数
source "${CLAUDE_PLUGIN_ROOT}/skills/_shared/ui/progress.sh"
for f in .claude/*/*.local.md; do
  echo "$f: $(yq eval '.parallel_execution.active_tasks' $f)"
done

# 2. 等待其他任务完成或手动清理孤儿任务
/Users/wenliang_zeng/.claude/scripts/ops/cleanup-orphans.sh

# 3. 检查 codeagent-wrapper
which codeagent-wrapper
```

#### 错误 2: 任务长时间无响应

**症状**: 任务状态一直显示 "🔄 运行中"，但超过预期时间

**原因**:

- 外部模型响应慢
- 网络不稳定
- 任务实际已失败但状态未更新（孤儿任务）

**解决方案**:

```bash
# 1. 查看任务日志
claude task output <task_id>

# 2. 清理孤儿任务
/Users/wenliang_zeng/.claude/scripts/ops/cleanup-orphans.sh

# 3. 重新启动工作流
```

#### 错误 3: SESSION_ID 提取失败

**症状**:

```
⚠️  无法提取 SESSION_ID，会话上下文可能丢失
```

**原因**:

- 外部模型输出格式变化
- 输出验证器规则过时

**影响**: 下次调用同一模型时，无法继续之前的会话上下文

**解决方案**:

```bash
# 1. 查看输出格式验证器
cat /Users/wenliang_zeng/.claude/skills/_shared/validation/output-validator.sh

# 2. 查看实际输出
cat .claude/developing/analysis-codex.md | head -20

# 3. 如果确认格式正确，更新验证器规则
```

#### 错误 4: 状态文件损坏

**症状**:

```
❌ 无法解析状态文件: .claude/developing.local.md
```

**原因**:

- YAML 格式错误
- 文件被外部程序修改
- 写入过程中断

**解决方案**:

```bash
# 1. 验证 YAML 格式
yq eval '.' .claude/developing.local.md

# 2. 如果无法修复，删除并重新开始
rm .claude/developing.local.md
/developing:dev-orchestrator
```

---

## 最佳实践

### 1. 合理使用并发槽位

**规则**: 全局最大并发数为 8

**建议**:

- 单个 orchestrator 通常使用 2-3 个槽位
- 不要同时启动多个大型工作流
- 及时清理完成的工作流

**查看当前使用情况**:

```bash
/Users/wenliang_zeng/.claude/scripts/ops/health-check.sh
```

### 2. 利用断点恢复

**何时使用**:

- 网络不稳定
- 长时间运行的工作流
- 需要中断后继续

**如何使用**:

- 工作流会自动保存状态
- 重新启动 orchestrator 时，会提示是否恢复
- 选择 "继续" 即可从中断处恢复

### 3. 监控健康状态

**定期检查**:

```bash
# 每天检查一次
/Users/wenliang_zeng/.claude/scripts/ops/health-check.sh

# 清理孤儿任务
/Users/wenliang_zeng/.claude/scripts/ops/cleanup-orphans.sh --dry-run
```

### 4. 理解 Codex vs Gemini 分工

| 场景         | 优先选择 | 原因                         |
| ------------ | -------- | ---------------------------- |
| 后端逻辑分析 | Codex    | 只读沙箱，复杂链路分析能力强 |
| 前端 UI/UX   | Gemini   | 快速原型，设计趋势敏感       |
| 安全审查     | Codex    | 安全模式分析，漏洞检测       |
| 用户体验审查 | Gemini   | UX 视角，可访问性检查        |
| 技术深度写作 | Codex    | 技术细节充分，代码示例完整   |
| 通俗易懂写作 | Gemini   | 生动表达，降低理解门槛       |

**并行策略**: 让两个模型各自发挥优势，然后由 Claude 合并结果

### 5. 选择合适的 Orchestrator

| 任务类型   | 推荐 Orchestrator         | 原因                  |
| ---------- | ------------------------- | --------------------- |
| 实现新功能 | dev-orchestrator          | 支持后端+前端并行生成 |
| 修复 Bug   | debug-orchestrator        | 多角度假设+修复方案   |
| 代码审查   | review-orchestrator       | 安全+UX双重审查       |
| 界面设计   | ui-ux-design-orchestrator | 3 种风格并行生成      |
| 编写测试   | test-orchestrator         | 后端+前端测试并行     |
| 规划任务   | plan-orchestrator         | 技术+可读性双重审查   |
| 创作内容   | social-post-orchestrator  | 技术+通俗双重草稿     |

---

## 故障排查

### 工具 1: 任务状态查询

```bash
/Users/wenliang_zeng/.claude/scripts/ops/task-status.sh
```

显示所有运行中的后台任务，包括 task_id、backend、运行时间。

### 工具 2: 孤儿任务清理

```bash
# 预览模式（不实际清理）
/Users/wenliang_zeng/.claude/scripts/ops/cleanup-orphans.sh --dry-run

# 执行清理
/Users/wenliang_zeng/.claude/scripts/ops/cleanup-orphans.sh
```

清理状态为 "running" 但实际已停止的任务。

### 工具 3: 健康检查

```bash
/Users/wenliang_zeng/.claude/scripts/ops/health-check.sh
```

检查：

- 依赖工具（yq, jq）
- 并发槽位使用情况
- 状态文件完整性

### 调试流程

1. **查看任务状态**

   ```bash
   /Users/wenliang_zeng/.claude/scripts/ops/task-status.sh
   ```

2. **查看任务输出**

   ```bash
   claude task output <task_id>
   ```

3. **查看状态文件**

   ```bash
   cat .claude/developing.local.md | yq eval '.'
   ```

4. **查看失败日志**

   ```bash
   cat .claude/logs/failed-tasks.log | grep <task_id>
   ```

5. **清理并重试**
   ```bash
   /Users/wenliang_zeng/.claude/scripts/ops/cleanup-orphans.sh
   rm .claude/developing.local.md
   /developing:dev-orchestrator
   ```

---

## 高级用法

### 手动触发并行执行

虽然 orchestrators 会自动管理并行执行，但你也可以手动触发（高级用法）：

```typescript
// 在自定义 skill 中使用声明式并行 API
await executeParallelPhase({
  domain: "custom",
  phaseName: "Phase X: 自定义并行任务",
  variables: {
    VAR1: "value1",
    VAR2: "value2",
  },
});
```

详见: `skills/_shared/orchestrator/parallel.md`

### 自定义进度显示

如果默认进度显示不满足需求，可以自定义：

```bash
source "${CLAUDE_PLUGIN_ROOT}/skills/_shared/ui/progress.sh"

# 自定义刷新间隔
monitor_progress ".claude/developing.local.md" 5  # 5秒刷新

# 自定义输出格式
custom_progress() {
  local active=$(yq eval '.parallel_execution.active_tasks' "$1")
  local completed=$(yq eval '.parallel_execution.completed_tasks' "$1")
  echo "⚡ $completed tasks done, $active running"
}

custom_progress ".claude/developing.local.md"
```

---

## 性能指标

基于基准测试结果（预估）：

| Orchestrator | 串行耗时 | 并行耗时 | 提速比 | 性能提升 |
| ------------ | -------- | -------- | ------ | -------- |
| dev          | ~14分钟  | ~7分钟   | 2.0x   | 50%      |
| debug        | ~11分钟  | ~5.5分钟 | 2.0x   | 50%      |
| test         | ~6分钟   | ~3分钟   | 2.0x   | 50%      |
| plan         | ~5分钟   | ~3分钟   | 1.7x   | 40%      |
| review       | ~6分钟   | ~3分钟   | 2.0x   | 50%      |
| social-post  | ~8分钟   | ~4分钟   | 2.0x   | 50%      |
| ui-ux-design | ~10分钟  | ~4分钟   | 2.5x   | 60%      |

**平均性能提升**: 50%

---

## 常见问题 (FAQ)

### Q1: 并行执行是否会增加成本？

**A**: 并行执行本身不增加调用次数，只是改变了调用顺序。总 API 调用次数与串行模式相同。

### Q2: 如何禁用并行执行？

**A**: 目前并行执行是自动启用的。如果需要串行模式，可以：

1. 使用不支持并行的 orchestrators（commit, image）
2. 或联系开发者添加禁用选项

### Q3: 并行执行是否支持超过 2 个模型？

**A**: 是的。ui-ux-design-orchestrator 支持 3 个并发任务。理论上可以支持更多，但受限于全局并发槽位（8个）。

### Q4: 断点恢复是否会丢失数据？

**A**: 不会。所有任务状态和输出都保存在状态文件中，恢复时会读取这些信息继续执行。

### Q5: 如何查看某个 orchestrator 的并行能力？

**A**: 查看 orchestrator 的 SKILL.md 文件，搜索 "并行执行支持" 章节。

---

## 相关文档

### 基础设施文档

- `skills/_shared/orchestrator/parallel.md` - 声明式并行 API
- `skills/_shared/background/adapter.md` - 后台任务适配层
- `skills/shared/workflow/STATE_FILE_V2.md` - V2 状态文件规范
- `skills/_shared/ui/progress-interface.md` - 统一进度显示接口

### Orchestrator 文档

- `agents/dev-orchestrator/SKILL.md`
- `agents/debug-orchestrator/SKILL.md`
- `agents/review-orchestrator/SKILL.md`
- `agents/ui-ux-design-orchestrator/SKILL.md`
- ... (其他 orchestrators)

### 用户指南

- `.claude/docs/best-practices.md` - 最佳实践
- `.claude/docs/troubleshooting.md` - 故障排查
- `.claude/skills/_shared/orchestrator/stage3-integration-summary.md` - 集成总结

---

## 反馈和支持

如有问题或建议，请：

1. 查看故障排查文档
2. 使用健康检查工具诊断
3. 联系开发团队

**版本**: 1.0.0
**最后更新**: 2026-01-13
