# Commit Orchestrator 测试计划

## 测试目标

验证 commit-orchestrator V2 Contract 实现的正确性、完整性和错误恢复能力。

## 测试环境

- 工作目录：`/Users/wenliang_zeng/.claude`
- Git 仓库：当前仓库
- 命令：`/commit` 和 `/ccg:commit`

## 测试类型

### 1. 端到端测试（E2E）

验证完整的提交流程，从初始化到最终提交。

#### 测试用例 1.1: 标准提交流程（/commit）

**前置条件**：

- 有未提交的代码变更
- 项目包含 package.json（Node.js 项目）

**执行步骤**：

1. 运行 `/commit`
2. 观察预检查（lint/build）执行
3. 观察变更收集和分析
4. 确认生成的提交信息
5. 完成提交

**预期结果**：

```
✅ 提交成功

提交信息: feat(docs): ✨ 添加测试计划文档
Commit Hash: [hash]
分支: [branch]
变更统计: 1 个文件，+XXX/-0 行

运行目录: .claude/committing/runs/[run-id]
```

**验证点**：

- [ ] run-initializer 创建运行目录
- [ ] precheck-runner 执行成功
- [ ] change-collector 收集变更
- [ ] change-analyzer 分析正确
- [ ] message-generator 生成规范消息
- [ ] commit-executor 执行成功
- [ ] 所有中间文件存在且格式正确

#### 测试用例 1.2: 纯 Git 提交流程（/ccg:commit）

**前置条件**：

- 有已暂存的代码变更

**执行步骤**：

1. 运行 `/ccg:commit`
2. 观察跳过预检查
3. 确认提交信息
4. 完成提交

**预期结果**：

- 跳过 Phase 0（precheck）
- 直接从 Phase 1 开始
- 无 precheck-result.json 文件

**验证点**：

- [ ] 跳过预检查
- [ ] 其他阶段正常执行
- [ ] 提交成功

#### 测试用例 1.3: 带选项的提交

**执行步骤**：

1. 运行 `/commit --type feat --scope api --emoji --issue 123`
2. 验证选项传递

**预期结果**：

- 提交类型强制为 feat
- 作用域为 api
- 包含 emoji ✨
- Footer 包含 "Closes #123"

**验证点**：

- [ ] 类型覆盖生效
- [ ] 作用域覆盖生效
- [ ] Emoji 显示正确
- [ ] Issue 关联正确

### 2. 错误场景测试

验证各种错误情况下的处理和恢复。

#### 测试用例 2.1: 预检查失败

**前置条件**：

- 代码存在 lint 错误

**执行步骤**：

1. 运行 `/commit`
2. 预检查失败

**预期行为**：

```
❌ 预检查失败

错误类型: lint_failed
错误信息: ESLint found 3 errors

建议: 修复 lint 错误后重试，或使用 --no-verify 跳过

是否继续提交？
1. 查看失败详情并修复
2. 跳过预检查继续（风险）
3. 中止提交
```

**验证点**：

- [ ] 检测到预检查失败
- [ ] 展示详细错误信息
- [ ] 提供恢复选项
- [ ] state.json 记录失败状态

#### 测试用例 2.2: 无暂存变更

**前置条件**：

- 无已暂存的变更（git add）

**执行步骤**：

1. 运行 `/commit`

**预期行为**：

```
❌ 无暂存变更

提示：无暂存变更，无法提交

是否暂存变更？
1. 自动暂存所有变更（git add -A）
2. 手动选择文件暂存
3. 中止提交
```

**验证点**：

- [ ] 检测到 has_staged == false
- [ ] 提供暂存选项
- [ ] 选择 1 后执行 git add -A
- [ ] 选择 3 后更新 state.json 状态

#### 测试用例 2.3: Git Hook 失败

**前置条件**：

- 配置 pre-commit hook 检查

**执行步骤**：

1. 创建会失败的 hook
2. 运行 `/commit`

**预期行为**：

```
❌ 提交失败

错误类型: pre_commit_hook_failed
错误信息: [hook output]

建议: 使用 --no-verify 跳过 hooks，或修复 hook 报告的问题

恢复选项：
1. 重试提交（修复错误后）
2. 跳过 hooks 提交 (/commit --no-verify)
3. 查看详细错误日志
```

**验证点**：

- [ ] 检测到 hook 失败
- [ ] 展示 hook 输出
- [ ] commit-result.json 记录错误类型
- [ ] 提供 --no-verify 建议

#### 测试用例 2.4: 建议拆分提交

**前置条件**：

- 有多个作用域的变更（如 components + utils）

**执行步骤**：

1. 暂存多作用域变更
2. 运行 `/commit`

**预期行为**：

```
🔍 变更分析完成

检测到以下情况建议拆分提交：
- 涉及 2 个作用域：components, utils
- 共 12 个文件

推荐拆分方案：
1. feat(components): 新增组件 (8 文件)
2. refactor(utils): 优化工具 (4 文件)

是否拆分提交？
1. 是，按建议拆分
2. 否，单次提交所有变更
3. 取消，稍后手动处理
```

**验证点**：

- [ ] should_split == true
- [ ] 展示拆分建议
- [ ] 提供选择
- [ ] changes-analysis.json 包含 split_recommendation

### 3. 状态恢复测试

验证中断恢复机制。

#### 测试用例 3.1: 中断恢复

**执行步骤**：

1. 运行 `/commit`
2. 在 Phase 2（分析阶段）中断
3. 读取 state.json 的 current_phase
4. 再次运行 `/commit`

**预期行为**：

- 从 Phase 2 继续执行
- Phase 0-1 不重复执行
- 使用已有的中间文件

**验证点**：

- [ ] 识别中断的阶段
- [ ] 跳过已完成阶段
- [ ] 从中断点继续
- [ ] 最终提交成功

#### 测试用例 3.2: 恢复失败阶段

**执行步骤**：

1. 让某个 Skill 失败（如 precheck）
2. state.json 记录 status: "failed"
3. 修复问题后重试

**预期行为**：

```
检测到上次运行失败于 Phase 0: precheck

是否重试？
1. 重试失败的阶段
2. 跳过该阶段（如适用）
3. 从头开始新运行
```

**验证点**：

- [ ] 识别失败阶段
- [ ] 提供重试选项
- [ ] 重试后更新状态

### 4. Skills 单元测试

验证每个 Skill 的独立功能。

#### 测试用例 4.1: precheck-runner

**输入**：

```bash
Skill("committing:precheck-runner",
     args="run_dir=.claude/committing/runs/test skip=false")
```

**预期输出**（precheck-result.json）：

```json
{
  "success": true,
  "skipped": false,
  "checks": [
    { "name": "lint", "passed": true, "duration_ms": 1234 },
    { "name": "build", "passed": true, "duration_ms": 5678 }
  ],
  "summary": { "total": 2, "passed": 2, "failed": 0 }
}
```

**验证点**：

- [ ] 检测包管理器
- [ ] 执行 lint 和 build
- [ ] 记录执行时间
- [ ] JSON 格式正确

#### 测试用例 4.2: change-collector

**输入**：

```bash
Skill("committing:change-collector",
     args="run_dir=.claude/committing/runs/test")
```

**预期输出**（changes-raw.json）：

```json
{
  "branch": "main",
  "staged": [
    {
      "status": "M",
      "path": "file.ts",
      "type": "modified",
      "file_type": "typescript",
      "scope": "root"
    }
  ],
  "diff_stat": { "files_changed": 1, "insertions": 10, "deletions": 0 },
  "has_staged": true
}
```

**验证点**：

- [ ] 解析 git status --porcelain
- [ ] 识别文件类型
- [ ] 提取作用域
- [ ] 统计变更行数

#### 测试用例 4.3: change-analyzer

**输入**：

```bash
Skill("committing:change-analyzer",
     args="run_dir=.claude/committing/runs/test changes_raw_path=.claude/committing/runs/test/changes-raw.json")
```

**预期输出**（changes-analysis.json）：

```json
{
  "primary_type": "feat",
  "primary_scope": "docs",
  "should_split": false,
  "commit_strategy": {
    "suggested_type": "feat",
    "suggested_scope": "docs",
    "confidence": "high"
  }
}
```

**验证点**：

- [ ] 映射变更类型到 commit 类型
- [ ] 识别主要作用域
- [ ] 评估拆分必要性
- [ ] 提供置信度

#### 测试用例 4.4: message-generator

**输入**：

```bash
Skill("committing:message-generator",
     args="run_dir=.claude/committing/runs/test changes_analysis_path=.claude/committing/runs/test/changes-analysis.json options='{\"emoji\": true}'")
```

**预期输出**（commit-message.md）：

```markdown
# Commit Message

## 标题

feat(docs): ✨ 添加测试计划文档

## 正文

变更文件清单:

**feat**:

- docs/commit-orchestrator-testing.md

变更统计: 1 个文件，+XXX/-0 行

## Footer

（无）
```

**验证点**：

- [ ] 标题长度 ≤72 字符
- [ ] Emoji 正确映射
- [ ] 正文格式规范
- [ ] Footer 处理正确

#### 测试用例 4.5: commit-executor

**输入**：

```bash
Skill("committing:commit-executor",
     args="run_dir=.claude/committing/runs/test commit_message_path=.claude/committing/runs/test/commit-message.md")
```

**预期输出**（commit-result.json）：

```json
{
  "success": true,
  "commit_hash": "abc1234567890abcdef1234567890abcdef12345",
  "commit_hash_short": "abc1234",
  "commit_message_title": "feat(docs): ✨ 添加测试计划文档",
  "files_committed": 1,
  "insertions": 100,
  "deletions": 0
}
```

**验证点**：

- [ ] 提取标题/正文/Footer
- [ ] 使用 HEREDOC 安全传递
- [ ] 执行 git commit
- [ ] 返回 commit hash

### 5. 性能测试

验证大规模变更场景下的性能。

#### 测试用例 5.1: 大量文件

**前置条件**：

- 暂存 100+ 个文件

**执行步骤**：

1. 运行 `/commit`
2. 记录每个阶段耗时

**预期**：

- change-collector: < 5s
- change-analyzer: < 3s
- message-generator: < 1s
- 总时间: < 15s

**验证点**：

- [ ] 无超时
- [ ] 所有 Skills 正常完成
- [ ] JSON 文件大小合理（< 1MB）

#### 测试用例 5.2: 大 diff

**前置条件**：

- 单个文件 1000+ 行变更

**执行步骤**：

1. 运行 `/commit`

**预期**：

- 正常生成提交信息
- 建议拆分（diff > 300 行）

**验证点**：

- [ ] diff 统计正确
- [ ] 触发拆分建议
- [ ] message 生成成功

## 测试执行记录

| 用例编号 | 用例名称          | 执行日期 | 结果 | 备注 |
| -------- | ----------------- | -------- | ---- | ---- |
| 1.1      | 标准提交流程      | -        | ⏳   | -    |
| 1.2      | 纯 Git 提交流程   | -        | ⏳   | -    |
| 1.3      | 带选项的提交      | -        | ⏳   | -    |
| 2.1      | 预检查失败        | -        | ⏳   | -    |
| 2.2      | 无暂存变更        | -        | ⏳   | -    |
| 2.3      | Git Hook 失败     | -        | ⏳   | -    |
| 2.4      | 建议拆分提交      | -        | ⏳   | -    |
| 3.1      | 中断恢复          | -        | ⏳   | -    |
| 3.2      | 恢复失败阶段      | -        | ⏳   | -    |
| 4.1      | precheck-runner   | -        | ⏳   | -    |
| 4.2      | change-collector  | -        | ⏳   | -    |
| 4.3      | change-analyzer   | -        | ⏳   | -    |
| 4.4      | message-generator | -        | ⏳   | -    |
| 4.5      | commit-executor   | -        | ⏳   | -    |
| 5.1      | 大量文件          | -        | ⏳   | -    |
| 5.2      | 大 diff           | -        | ⏳   | -    |

## 回归测试清单

每次修改后必须通过的核心测试：

- [ ] 1.1 标准提交流程
- [ ] 1.2 纯 Git 提交流程
- [ ] 2.2 无暂存变更
- [ ] 3.1 中断恢复

## 测试工具

### 手动测试

直接在当前仓库执行命令：

```bash
# 测试完整流程
/commit

# 测试纯 Git 流程
/ccg:commit

# 测试带选项
/commit --type feat --scope test --emoji
```

### 验证脚本

```bash
# 验证运行目录结构
run_dir=$(ls -t .claude/committing/runs/ | head -1)
ls -la .claude/committing/runs/$run_dir/

# 验证 state.json
cat .claude/committing/runs/$run_dir/state.json | jq '.'

# 验证所有产物文件
for file in precheck-result.json changes-raw.json changes-analysis.json commit-message.md commit-result.json; do
    if [ -f ".claude/committing/runs/$run_dir/$file" ]; then
        echo "✅ $file 存在"
    else
        echo "❌ $file 缺失"
    fi
done
```

## 已知问题

暂无

## 测试结论

待测试完成后填写
