#!/bin/bash
# UserPromptSubmit hook - 注入可用 Skills，让模型自行判断

PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-$(dirname "$0")/../../..}"

cat << 'EOF'
## 可用工作流 Skills

根据用户意图选择合适的 Skill 调用：

| Skill | 说明 | 适用场景 |
|-------|------|----------|
| `ccg-core:commit` | 规范提交 | 提交代码、git commit、推送变更 |
| `ccg-core:debug` | 调试修复 | 修 bug、报错、异常、崩溃、排查问题 |
| `ccg-core:dev` | 开发实现 | 实现功能、重构、优化、添加特性 |
| `ccg-core:review` | 代码审查 | 审查代码、PR review、代码质量检查 |
| `ccg-core:test` | 测试生成 | 写测试、单测、用例、覆盖率 |
| `ccg-core:plan` | 规划设计 | 技术方案、架构设计、任务规划 |
| `ccg-core:init` | 项目分析 | 分析项目结构、技术栈检测、迁移评估 |

**扩展 Skills**（如已安装）：

| Skill | 说明 |
|-------|------|
| `ccg-writing:article` | 文章写作 |
| `ccg-writing:social-post` | 社交媒体帖子 |
| `ccg-ui:image` | 图像生成 |
| `ccg-ui:ui-design` | UI/UX 设计 |

---

**🔴 执行规则**：

1. **识别执行意图时必须调用 Skill**：如果用户想要执行操作（提交/调试/开发/测试/审查/规划等），使用 `Skill("ccg-core:xxx")` 而非直接用 Bash/Read/Write 等工具
2. **询问/理解类可直接回答**：解释概念、回答问题、学习性质的请求无需调用 Skill
3. **不确定时询问用户**：多个 Skill 都可能适用时，列出选项让用户选择

**判断示例**：
- "提交一下" → `Skill("ccg-core:commit")`
- "这个 bug 怎么修" → `Skill("ccg-core:debug")`
- "帮我实现一个登录功能" → `Skill("ccg-core:dev")`
- "看看这段代码有什么问题" → `Skill("ccg-core:review")`
- "什么是 React hooks" → 直接回答（理解类）
- "优化一下性能" → 可能是 dev 或 debug，询问用户具体需求
EOF
