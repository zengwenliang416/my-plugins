---
description: "UI/UX 设计工作流：需求分析 → 样式推荐 → 设计生成（并行 3 变体）→ UX 检查 → 代码生成 → 质量验证。支持图片参考。"
argument-hint: "[--image=<path>] [--scenario=from_scratch|optimize] [--tech-stack=react|vue] [--run-id=xxx] <设计描述>"
allowed-tools:
  - Skill
  - AskUserQuestion
  - Read
  - Write
  - Bash
  - Task
---

# /ui-design - UI/UX 设计工作流命令

## 🚨🚨🚨 强制执行规则（不可跳过）

**你必须按照下面的 Phase 顺序，使用 Skill 工具调用对应的 skill。**

**禁止行为（违反则工作流失败）：**

- ❌ 跳过 Skill 调用，自己直接写代码或分析
- ❌ 用 Read/Write/Bash 替代 Skill 调用
- ❌ 省略任何 Phase
- ❌ Phase 3 跳过 auggie-mcp 或 LSP 分析
- ❌ Phase 4 不生成 HTML 预览文件

**每个 Phase 你必须：**

1. 调用指定的 Skill（使用 Skill 工具）
2. 等待 Skill 执行完成
3. **验证输出文件存在**（见各 Phase 验证清单）
4. 再进入下一个 Phase

### 执行模型

```
┌─────────────────────────────────────────────────────────────────┐
│  自动执行（无需询问）    │  硬停止（必须询问）                  │
├─────────────────────────────────────────────────────────────────┤
│  Phase 1 → Phase 2      │  ⏸️ Phase 2: 场景确认               │
│  Phase 2.5（如有图片）  │  ⏸️ Phase 5: 方案选择               │
│  Phase 3 → Phase 4      │                                      │
│  Phase 6 → Phase 7      │                                      │
│  Phase 7 → Phase 8      │                                      │
│  Phase 8 → Phase 9      │                                      │
│  Phase 9 → Phase 10     │                                      │
└─────────────────────────────────────────────────────────────────┘
```

**🚨 核心规则：除了 Phase 2 和 Phase 5，其他阶段之间不需要询问，直接继续！**

### 阶段流程

```
Phase 1: 初始化        → 创建 RUN_DIR
Phase 2: 场景确认      → AskUserQuestion（⏸️ 硬停止）
Phase 2.5: 图片分析    → Skill("image-analyzer")【仅当有 --image 参数时】
                       → Gemini 多轮分析 → 自动继续 ↓
Phase 3: 需求分析      → Skill("requirement-analyzer") → 自动继续 ↓
Phase 4: 样式推荐      → Skill("style-recommender") → 自动继续 ↓
Phase 5: 方案选择      → AskUserQuestion（⏸️ 硬停止）
Phase 6: 设计生成      → Skill("design-variant-generator") × 3 并行 → 自动继续 ↓
Phase 7: UX 检查       → Skill("ux-guideline-checker") → 自动继续 ↓
                       ├─ 通过 → Phase 8
                       └─ 失败 → 返回 Phase 6 重新生成
Phase 8: 代码生成      → Skill("code-generator") → 自动继续 ↓
Phase 9: 质量验证      → Skill("quality-validator") → 自动继续 ↓
Phase 10: 交付         → 输出摘要
```

### ⚠️ 关键规则

1. **自动继续原则**：除硬停止点外，Phase 完成后**立即**执行下一个 Phase
2. **硬停止点**：只有 Phase 2 和 Phase 5 需要 AskUserQuestion
3. **不询问 "是否继续"**：Phase 3/4/6/7/8/9 完成后，直接执行下一阶段
4. **并行执行**：Phase 6 使用 Task 并行生成 3 个设计变体
5. **重试机制**：Phase 7 失败时回退到 Phase 6
6. **双模型协作**：Phase 8 使用 Gemini + Claude 协作

---

## Phase 1: 初始化

### 参数解析

| 选项                 | 说明                             | 默认值       |
| -------------------- | -------------------------------- | ------------ |
| `--image=<path>`     | 参考图片路径（启用图片分析）     | -            |
| `--scenario=value`   | 设计场景 (from_scratch/optimize) | from_scratch |
| `--tech-stack=value` | 技术栈 (react/vue)               | react        |
| `--run-id=<id>`      | 使用指定 run-id（断点续传）      | -            |

### 解析逻辑

```bash
# 初始化选项对象
OPTIONS='{}'
IMAGE_PATH=""

# 解析各选项
[[ "$ARGUMENTS" =~ --image=([^ ]+) ]] && IMAGE_PATH="${BASH_REMATCH[1]}" && OPTIONS=$(echo "$OPTIONS" | jq --arg v "${IMAGE_PATH}" '. + {image_path: $v}')
[[ "$ARGUMENTS" =~ --scenario=([^ ]+) ]] && OPTIONS=$(echo "$OPTIONS" | jq --arg v "${BASH_REMATCH[1]}" '. + {scenario: $v}')
[[ "$ARGUMENTS" =~ --tech-stack=([^ ]+) ]] && OPTIONS=$(echo "$OPTIONS" | jq --arg v "${BASH_REMATCH[1]}" '. + {tech_stack: $v}')

# 提取设计描述（排除选项后的剩余部分）
DESCRIPTION=$(echo "$ARGUMENTS" | sed -E 's/--[a-zA-Z-]+(=[^ ]+)?//g' | xargs)

# 标记是否有图片参考
HAS_IMAGE=$([[ -n "$IMAGE_PATH" ]] && echo "true" || echo "false")
```

### 运行目录创建

```bash
if [[ "$ARGUMENTS" =~ --run-id=([^ ]+) ]]; then
    RUN_ID="${BASH_REMATCH[1]}"
    RUN_DIR=".claude/ui-design/runs/${RUN_ID}"
    if [ ! -d "$RUN_DIR" ]; then
        echo "❌ 错误: 运行目录不存在: $RUN_DIR"
        exit 1
    fi
    MODE="resume"
else
    MODE="new"
    RUN_ID=$(date -u +%Y%m%dT%H%M%SZ)
    RUN_DIR=".claude/ui-design/runs/${RUN_ID}"
    mkdir -p "$RUN_DIR"
fi
```

### 创建状态文件

```bash
if [ "$MODE" = "new" ]; then
    cat > "${RUN_DIR}/state.json" << EOF
{
  "domain": "ui-design",
  "workflow_id": "${RUN_ID}",
  "goal": "${DESCRIPTION}",
  "options": ${OPTIONS},
  "phases": [
    {"id": "initialization", "status": "completed"},
    {"id": "scenario-confirm", "status": "pending"},
    {"id": "requirement-analysis", "status": "pending"},
    {"id": "style-recommendation", "status": "pending"},
    {"id": "variant-selection", "status": "pending"},
    {"id": "design-generation", "status": "pending"},
    {"id": "ux-check", "status": "pending"},
    {"id": "code-generation", "status": "pending"},
    {"id": "quality-validation", "status": "pending"},
    {"id": "delivery", "status": "pending"}
  ],
  "created_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
}
EOF

    # 写入设计描述
    echo "$DESCRIPTION" > "${RUN_DIR}/input.md"
fi
```

**🚨 完成后立即执行 Phase 2，不要停止！**

---

## Phase 2: 场景确认

### ⏸️ 硬停止

**使用 AskUserQuestion 确认设计场景：**

```
问题 1: 设计场景是什么？
选项:
  - 从零设计（全新项目）
  - 优化现有（改进现有界面）

问题 2: 技术栈偏好？
选项:
  - React + Tailwind（推荐）
  - Vue + Tailwind
```

**验证**：用户确认后更新 `${RUN_DIR}/state.json` 中的 options。

**🚨 用户确认后：**
- 如果有 `--image` 参数 → 执行 Phase 2.5
- 如果没有 → 跳过 Phase 2.5，直接执行 Phase 3

---

## Phase 2.5: 图片分析（仅当有 --image 参数）

### 🚨🚨🚨 强制执行 - Gemini 多轮分析 🚨🚨🚨

**触发条件**：用户提供了 `--image=<path>` 参数

**❌ 禁止行为（违反则工作流失败）：**
- ❌ 跳过 Gemini 图片分析
- ❌ 自己猜测图片内容
- ❌ 只做一轮分析
- ❌ 不保存 Gemini 原始回答

**✅ 唯一正确做法：调用 Skill 工具**

### 立即执行（不要犹豫）

**判断是否执行：**
```
if HAS_IMAGE == "true":
    → 执行 Phase 2.5
else:
    → 跳过，直接进入 Phase 3
```

**立即调用 Skill：**
```
Skill(skill="image-analyzer", args="run_dir=${RUN_DIR} image_path=${IMAGE_PATH}")
```

**关键**：`image-analyzer` Skill **会**执行以下操作：
- 使用 `gemini` 命令读取图片
- 进行 **4 轮 Gemini 分析**：
  1. 整体风格分析
  2. 配色系统分析
  3. 组件识别
  4. 字体排版分析
- Claude 综合 Gemini 回答，生成可执行设计规格
- 输出 `${run_dir}/image-analysis.md`

**产出**：
- 提取的配色系统（HEX 值 + Tailwind 等效）
- 识别的组件清单
- 字体排版规格
- 设计 Token JSON

### 验证检查清单（必须全部通过）

**Phase 2.5 完成后，你必须验证：**
- [ ] `${RUN_DIR}/image-analysis.md` 已生成
- [ ] 文档包含 "配色系统" 表格
- [ ] 文档包含 "组件清单" 表格
- [ ] 文档包含 "Gemini 分析原始记录" 部分

**如果验证失败，不要继续，报告错误！**

### ⏩ 自动继续到 Phase 3

**验证通过后，不询问用户，直接执行：**
```
→ 输出 "✅ 图片分析完成，提取了 X 种颜色、Y 个组件..."
→ 立即调用 Phase 3
```

---

## Phase 3: 需求分析（auggie-mcp + LSP）

### 🚨🚨🚨 强制执行 - 禁止跳过 🚨🚨🚨

**❌ 禁止行为（违反则工作流失败）：**
- ❌ 自己用 Read 读文件然后分析
- ❌ 跳过 auggie-mcp 代码检索
- ❌ 跳过 LSP 组件分析（如果存在组件）
- ❌ 说"我来分析需求"然后自己写分析

**✅ 唯一正确做法：调用 Skill 工具**

### 立即执行（不要犹豫）

**立即调用 Skill：**
```
Skill(skill="requirement-analyzer", args="run_dir=${RUN_DIR} description=${DESCRIPTION}")
```

**关键**：`requirement-analyzer` Skill **会**执行以下操作：
- 使用 `mcp__auggie-mcp__codebase-retrieval` 检索现有代码上下文
- **如果发现组件文件**：使用 `LSP(operation="documentSymbol")` 和 `LSP(operation="hover")` 分析组件结构
- 从用户描述提取：产品类型、核心功能、目标用户、设计偏好
- 如信息不足，使用 AskUserQuestion 补充

### 验证检查清单（必须全部通过）

**Phase 3 完成后，你必须验证：**
- [ ] `${RUN_DIR}/requirements.md` 已生成
- [ ] 如果是 optimize 场景，requirements.md 包含 "现有代码分析" 部分
- [ ] 如果发现了组件文件，requirements.md 包含 "现有组件" 表格

**如果验证失败，不要继续，报告错误！**

### ⏩ 自动继续到 Phase 4

**验证通过后，不询问用户，直接执行：**
```
→ 输出 "✅ 需求分析完成，进入样式推荐阶段..."
→ 立即调用 Phase 4
```

---

## Phase 4: 样式推荐（生成 HTML 预览）

### 🚨🚨🚨 强制执行 - 禁止跳过 🚨🚨🚨

**❌ 禁止行为（违反则工作流失败）：**
- ❌ 自己用 Write 写推荐文档
- ❌ 跳过 HTML 预览生成
- ❌ 说"我来生成推荐"然后自己写
- ❌ 只生成 markdown 不生成 HTML 预览

**✅ 唯一正确做法：调用 Skill 工具**

### 立即执行（不要犹豫）

**立即调用 Skill：**
```
Skill(skill="style-recommender", args="run_dir=${RUN_DIR}")
```

**关键**：`style-recommender` Skill **会**执行以下操作：
- 匹配预定义的样式/配色/字体组合
- 生成 3 套设计方案（A 稳妥、B 大胆、C 平衡）
- **使用 Bash 生成 HTML 预览文件**（这是必须的！）
- 输出完整的 `${RUN_DIR}/previews/` 目录

**产出**：
- 方案 A: 稳妥专业型（样式 + 配色 + 字体）
- 方案 B: 创意大胆型（样式 + 配色 + 字体）
- 方案 C: 混合平衡型（样式 + 配色 + 字体）
- **HTML 预览页面（必须生成）**

### 验证检查清单（必须全部通过）

**Phase 4 完成后，你必须验证：**
- [ ] `${RUN_DIR}/style-recommendations.md` 已生成
- [ ] `${RUN_DIR}/previews/index.html` 已生成
- [ ] `${RUN_DIR}/previews/preview-A.html` 已生成
- [ ] `${RUN_DIR}/previews/preview-B.html` 已生成
- [ ] `${RUN_DIR}/previews/preview-C.html` 已生成

**如果任何 HTML 预览文件未生成，这是 Skill 失败，需要报告错误！**

### ⏩ 自动继续到 Phase 5

**验证通过后，直接进入 Phase 5（硬停止）：**
```
→ 输出 "✅ 样式推荐完成，HTML 预览页面已生成！"
→ 输出预览文件路径
→ 提示用户在浏览器中打开预览
→ 调用 AskUserQuestion 让用户选择方案
```

---

## Phase 5: 方案选择

### ⏸️ 硬停止

**Step 1: 提示用户查看 HTML 预览**

```
🎨 设计方案已生成！请在浏览器中预览：

📂 预览文件位置：
   ${RUN_DIR}/previews/index.html

🌐 快速打开（macOS）：
   open ${RUN_DIR}/previews/index.html

💡 预览页面支持：
   - Tab 切换查看 3 种不同风格
   - 包含 Header, Hero, Cards, Form, Footer 完整示例
   - 实际应用 Tailwind 配色和字体
```

**Step 2: 用户查看后，使用 AskUserQuestion 询问选择**

```
📋 设计方案推荐（已生成 HTML 预览）

方案 A: 稳妥专业型 ⭐ 推荐
  - 样式: ${style_A}
  - 配色: ${color_A}
  - 字体: ${font_A}
  - 预览: ${RUN_DIR}/previews/preview-A.html

方案 B: 创意大胆型
  - 样式: ${style_B}
  - 配色: ${color_B}
  - 字体: ${font_B}
  - 预览: ${RUN_DIR}/previews/preview-B.html

方案 C: 混合平衡型
  - 样式: ${style_C}
  - 配色: ${color_C}
  - 字体: ${font_C}
  - 预览: ${RUN_DIR}/previews/preview-C.html

请选择要生成详细设计的方案：
  - 生成全部 3 个方案（并行）（推荐）
  - 仅生成方案 A
  - 仅生成方案 B
  - 仅生成方案 C
```

**记录选择**：将用户选择写入 `${RUN_DIR}/selected-variants.txt`

**🚨 用户确认后立即执行 Phase 6，不要停止！**

---

## Phase 6: 设计生成（并行执行）

### 🚨 强制执行 - 并行模式

**根据用户选择，并行调用 Skill：**

```
# 如果用户选择"生成全部 3 个方案"
Task(
  subagent_type="general-purpose",
  description="Generate design variant A",
  prompt="Skill(skill=\"design-variant-generator\", args=\"run_dir=${RUN_DIR} variant_id=A\")",
  run_in_background=true
)
Task(
  subagent_type="general-purpose",
  description="Generate design variant B",
  prompt="Skill(skill=\"design-variant-generator\", args=\"run_dir=${RUN_DIR} variant_id=B\")",
  run_in_background=true
)
Task(
  subagent_type="general-purpose",
  description="Generate design variant C",
  prompt="Skill(skill=\"design-variant-generator\", args=\"run_dir=${RUN_DIR} variant_id=C\")",
  run_in_background=true
)

# 等待所有任务完成
```

**如果用户只选择单个方案**：
```
Skill(skill="design-variant-generator", args="run_dir=${RUN_DIR} variant_id=${SELECTED}")
```

**验证**：确认 `${RUN_DIR}/design-{A,B,C}.md` 已生成

**🚨 完成后立即执行 Phase 7，不要停止！**

---

## Phase 7: UX 检查（带重试）

### 🚨 强制执行

**对每个生成的设计变体执行 UX 检查：**

```
for variant in selected_variants:
    Skill(skill="ux-guideline-checker", args="run_dir=${RUN_DIR} variant_id=${variant}")
```

**验证**：确认 `${RUN_DIR}/ux-check-report.md` 已生成

**检查结果判定**：
- **通过条件**：通过率 ≥ 80% 且高优先级问题 = 0
- **失败处理**：如果失败，返回 Phase 6 重新生成该变体

**重试逻辑**：
```
max_retries = 2
for variant in failed_variants:
    if retry_count < max_retries:
        # 根据 UX 检查报告中的建议，重新生成设计
        Skill(skill="design-variant-generator", args="run_dir=${RUN_DIR} variant_id=${variant} fixes=${ux_issues}")
        Skill(skill="ux-guideline-checker", args="run_dir=${RUN_DIR} variant_id=${variant}")
        retry_count++
    else:
        # 超过重试次数，使用 AskUserQuestion 询问用户
        AskUserQuestion("UX 检查多次失败，是否继续使用当前设计？")
```

**🚨 所有变体通过后立即执行 Phase 8，不要停止！**

---

## Phase 8: 代码生成（双模型协作）

### 🚨🚨🚨 强制执行 - 必须使用 Gemini 🚨🚨🚨

**❌ 禁止行为（违反则工作流失败）：**
- ❌ 跳过 Skill 调用，自己直接写代码
- ❌ 跳过 Gemini，自己生成代码
- ❌ 用 Read 读现有文件然后自己修改（而不是调用 Skill）
- ❌ 说 "我来生成代码" 然后自己写

**✅ 唯一正确做法：调用 Skill 工具**

**使用 AskUserQuestion 让用户选择最终方案（如有多个）：**
```
如果有多个通过 UX 检查的方案:
    AskUserQuestion("请选择要生成代码的最终方案: A / B / C")
```

**立即调用 Skill：**
```
Skill(skill="code-generator", args="run_dir=${RUN_DIR} variant_id=${FINAL_VARIANT} tech_stack=${TECH_STACK}")
```

**关键**：`code-generator` Skill **必须**执行以下操作：
1. 使用 `mcp__auggie-mcp__codebase-retrieval` 分析现有代码
2. **如果发现组件文件**：使用 `LSP` 分析组件结构
3. **🚨🚨🚨 必须使用 codeagent-wrapper gemini 生成原型**（不可跳过！）
4. Claude 重构 Gemini 的输出

**双模型协作流程**（Skill 内部执行）：
1. **Gemini 生成原型**：快速生成完整代码结构（gemini-raw/）
2. **Claude 重构精简**：移除冗余、统一命名、补全类型（refactored/ → final/）

### 验证检查清单（必须全部通过）

**Phase 8 完成后，你必须验证：**
- [ ] `${RUN_DIR}/code/gemini-raw/` 目录已生成（Gemini 原型）
- [ ] `${RUN_DIR}/code/refactored/` 目录已生成（Claude 重构版）
- [ ] `${RUN_DIR}/code/${TECH_STACK}/` 目录已生成（最终版本）

**如果 gemini-raw/ 目录不存在，说明没有使用 Gemini，这是 Skill 失败！**

**🚨 完成后立即执行 Phase 9，不要停止！**

---

## Phase 9: 质量验证

### 🚨 强制执行

**立即调用 Skill：**
```
Skill(skill="quality-validator", args="run_dir=${RUN_DIR} variant_id=${FINAL_VARIANT} tech_stack=${TECH_STACK}")
```

**验证**：确认 `${RUN_DIR}/quality-report.md` 已生成

**检查结果判定**：
- **通过条件**：总分 ≥ 7.5/10
- **失败处理**：展示报告，询问用户是否接受当前质量

**🚨 完成后立即执行 Phase 10 交付！**

---

## Phase 10: 交付

输出完成摘要：

```
🎉 UI/UX 设计完成！

📋 任务: ${DESCRIPTION}
⏱️ 耗时: ${ELAPSED_TIME}
🎨 选定方案: 方案 ${FINAL_VARIANT}
🔧 技术栈: ${TECH_STACK}

📊 质量指标:
- UX 通过率: ${UX_PASS_RATE}%
- 质量评分: ${QUALITY_SCORE}/10
- 设计还原度: ${RESTORATION_RATE}%

📁 产物:
  ${RUN_DIR}/
  ├── requirements.md           # 需求分析
  ├── style-recommendations.md  # 样式推荐
  ├── design-${FINAL_VARIANT}.md  # 最终设计规格
  ├── ux-check-report.md        # UX 检查报告
  ├── code/${TECH_STACK}/       # 生成代码
  │   ├── components/
  │   ├── pages/
  │   ├── styles/
  │   └── package.json
  └── quality-report.md         # 质量报告

🔄 后续:
  - 断点续传: /ui-design --run-id=${RUN_ID}
  - 安装依赖: cd ${RUN_DIR}/code/${TECH_STACK} && npm install
  - 启动开发: npm run dev
```

---

## 运行目录结构

```
.claude/ui-design/runs/20260115T100000Z/
├── state.json                 # 工作流状态
├── input.md                   # 原始输入
├── requirements.md            # Phase 3 产出
├── style-recommendations.md   # Phase 4 产出
├── previews/                  # Phase 4 产出（HTML 预览）
│   ├── index.html             # 对比总览页面
│   ├── preview-A.html         # 方案 A 预览
│   ├── preview-B.html         # 方案 B 预览
│   └── preview-C.html         # 方案 C 预览
├── selected-variants.txt      # Phase 5 用户选择
├── design-A.md                # Phase 6 产出（并行）
├── design-B.md                # Phase 6 产出（并行）
├── design-C.md                # Phase 6 产出（并行）
├── ux-check-report.md         # Phase 7 产出
├── code/                      # Phase 8 产出
│   └── react-tailwind/
│       ├── components/
│       ├── pages/
│       ├── styles/
│       ├── tailwind.config.js
│       ├── package.json
│       └── README.md
└── quality-report.md          # Phase 9 产出
```

---

## 错误处理

### 需求信息不足

```
⚠️ 需求信息不完整

缺失字段:
- 产品类型: 未明确
- 核心功能: 未明确

请补充以下信息...
```

### UX 检查失败

```
⚠️ UX 检查未通过

方案 ${VARIANT}:
  通过率: 72% (需要 ≥ 80%)
  高优先级问题: 3 个

正在根据建议重新生成设计方案...
重试次数: ${RETRY_COUNT}/2
```

### 质量验证失败

```
⚠️ 质量验证未通过

总分: 6.5/10 (需要 ≥ 7.5)
等级: C (勉强)

高优先级问题:
1. TypeScript 编译失败
2. 颜色值匹配率 <75%

建议:
1. 修复编译错误
2. 检查配色实现
```

---

## 约束

- 不跳过任何 Phase
- 每个 Phase 必须调用对应的 Skill
- 硬停止点必须等待用户确认
- Phase 6 并行执行设计生成
- Phase 7 失败需要重试（最多 2 次）
- Phase 8 使用双模型协作（Gemini + Claude）
