{
  "hooks": [
    {
      "event": "PreToolUse",
      "type": "bash",
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/enforce-tools.sh",
      "description": "强制 ui-design 工作流使用指定工具（auggie-mcp, codeagent-wrapper gemini）"
    }
  ]
}
