# 决策树：docflow-update-doc

## 文档更新路径判断

[是否提供有效变更输入]
├── 否：返回阻塞并要求补充变更范围。
└── 是：继续
    └── [llmdoc 是否存在]
        ├── 否：建议先执行 `/prompts:docflow-init-doc`。
        └── 是：继续
            └── [更新模式]
                ├── 增量：只更新受影响文档并保持最小改动。
                └── 全量：重扫目录并同步 index。
                    └── [是否有未覆盖风险]
                        ├── 是：输出风险与人工复核建议。
                        └── 否：输出完成报告。
